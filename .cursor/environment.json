{
  "name": "Arizona Deal Agent",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -e '.[dev]'",
  "terminals": [
    {
      "name": "api",
      "command": ".venv/bin/uvicorn arizona_deal_agent.web.app:app --host 0.0.0.0 --port 8000 --reload"
    }
  ],
  "ports": [8000]
}
